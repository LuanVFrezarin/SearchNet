{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm ci",
  "env": {
    "NEXT_PUBLIC_API_URL": {
      "value": "http://localhost:3001",
      "description": "API Backend URL (altere para seu Fly.io URL em produção)"
    }
  }
}
